{
  "name": "@rwx/solidjs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run with-env vinxi dev --host",
    "build": "bun run with-env vinxi build",
    "start": "bun run with-env vinxi start",
    "version": "vinxi version",
    "clean": "git clean -xdf .cache .turbo node_modules .vercel .vinxi",
    "typecheck": "tsc --customConditions disable",
    "with-env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@rwx/api": "workspace:*",
    "@rwx/ui": "workspace:*",
    "@solidjs/meta": "^0.29.4",
    "@solidjs/router": "^0.15.3",
    "@solidjs/start": "^1.1.3",
    "openapi-fetch": "^0.13.5",
    "solid-js": "^1.9.5",
    "vinxi": "^0.5.3"
  },
  "devDependencies": {
    "@rwx/tailwind-config": "workspace:*",
    "@rwx/tsconfig": "workspace:*",
    "@tailwindcss/postcss": "^4.0.17",
    "@tailwindcss/typography": "^0.5.16",
    "dotenv-cli": "^8.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
