export class PostFiltersDto {
  // page: number = 1;
  // limit: number = 10;
  // searchTerm?: string;
  // category?: string;
  // tags?: string[];
  // authorId?: string;
  // startDate?: Date;
  // endDate?: Date;
  // sortBy?: 'date' | 'title' | 'views';
  // sortOrder?: 'asc' | 'desc';
  // status?: 'draft' | 'published' | 'archived';
  // featured?: boolean;
}
