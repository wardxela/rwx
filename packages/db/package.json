{
  "name": "@rwx/db",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "types": "dist/index.ts",
  "scripts": {
    "build": "prisma generate",
    "clean": "git clean -xdf .cache .turbo dist node_modules"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "prisma-kysely": "^1.8.0"
  }
}
